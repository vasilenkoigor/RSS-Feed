#include "LLReactiveMatchers.xcconfig"
FRAMEWORK_SEARCH_PATHS = ${LLREACTIVEMATCHERS_FRAMEWORK_SEARCH_PATHS}
GCC_PREPROCESSOR_DEFINITIONS = $(inherited) COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/Headers/Private" "${PODS_ROOT}/Headers/Private/LLReactiveMatchers" "${PODS_ROOT}/Headers/Public"
OTHER_LDFLAGS = ${LLREACTIVEMATCHERS_OTHER_LDFLAGS}
PODS_ROOT = ${SRCROOT}
SKIP_INSTALL = YES